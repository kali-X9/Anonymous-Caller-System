sip:
  domain: "anonymous.sip"
  username: "user"
  password: "password"
  port: 5060

tor:
  restart_interval: 10

logging:
  level: INFO
  log_file: logs/app.log
  max_bytes: 10485760  # 10MB
  backup_count: 5

network:
  interface: eth0
